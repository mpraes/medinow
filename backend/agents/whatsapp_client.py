# This file will contain the logic to interact with the WhatsApp API (e.g., Twilio).

def send_whatsapp_message(to, body):
    """Sends a message to a WhatsApp user."""
    # In a real implementation, this would use the Twilio client to send a message.
    print(f"--- SENDING WHATSAPP MESSAGE ---")
    print(f"TO: {to}")
    print(f"BODY: {body}")
    print(f"-----------------------------")
    # Simulate success
    return True
